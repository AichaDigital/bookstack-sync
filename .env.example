# BookStack API Configuration
# These credentials can be obtained from your BookStack user settings
# (User Profile -> Access Tokens)

# Required: Your BookStack instance URL
BOOKSTACK_URL=https://your-bookstack.example.com

# Required: API Token ID
BOOKSTACK_TOKEN_ID=your-token-id-here

# Required: API Token Secret
BOOKSTACK_TOKEN_SECRET=your-token-secret-here

# Alternative names (for compatibility)
# WIKI_URL=https://your-bookstack.example.com
# WIKI_TOKEN_ID=your-token-id-here
# WIKI_TOKEN=your-token-secret-here

# Optional: Default book ID for sync operations
# BOOKSTACK_BOOK_ID=123

# Optional: Local Markdown directory path
# BOOKSTACK_MARKDOWN_PATH=docs

# Optional: Sync direction (push, pull, bidirectional)
# BOOKSTACK_SYNC_DIRECTION=push

# Optional: Conflict resolution (local, remote, newest, manual)
# BOOKSTACK_CONFLICT_RESOLUTION=manual

# Optional: API timeout in seconds
# BOOKSTACK_TIMEOUT=30

# Optional: Enable dry run mode (no actual changes)
# BOOKSTACK_DRY_RUN=false

# Optional: Enable logging
# BOOKSTACK_LOGGING=true
# BOOKSTACK_LOG_CHANNEL=stack
